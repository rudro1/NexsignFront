# NexsignFront

NexSign frontend — React 18 + Vite + TailwindCSS.

## Local dev

```bash
npm install
cp .env.example .env   # fill Firebase + API URL
npm run dev
```

Default API: `http://localhost:5001/api`

## Env vars

See `.env.example` for `VITE_API_BASE_URL` and Firebase keys.
